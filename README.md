
## Build Instructions

To ensure reproducible builds, use `npm ci` to install dependencies:

```bash
npm ci
```

After installation, it is recommended to run `npm audit fix` to address any security vulnerabilities:

```bash
npm audit fix
```
